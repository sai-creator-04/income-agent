services:
  - type: web
    name: income-agent
    runtime: python
    buildCommand: pip install requests python-dotenv
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9